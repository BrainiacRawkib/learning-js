// the need to check if a variable has been declared
// isn't necessary in ES6

if (typeof x === 'undefined'){
    console.log("x doesn't exist or is undefined!");
} else {
    // safe to refer to x....
}